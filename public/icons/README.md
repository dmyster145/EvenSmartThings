# Glasses display icons

Place optional PNG overrides here using these exact names:

- **ThumbsUp.png**: shown when all actions succeed
- **ThumbsDown.png**: shown when all actions fail
- **ThisClose.png**: shown when results are mixed (partial success)

Each icon is scaled to `25×25` for the confirmation container in the lower-right corner.
If any file is missing, the app falls back to built-in placeholder graphics.
